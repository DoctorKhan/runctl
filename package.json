{
  "name": "runctl",
  "version": "0.1.0",
  "description": "Runctl — .run/ + ~/.run port/PID helpers, JS dev wiring (Node ≥18), env manifest expander",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/DoctorKhan/devport-kit.git"
  },
  "license": "MIT",
  "keywords": ["runctl", "dev", "ports", "pid", "vercel", "dotenv"],
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "runctl": "bin/runctl"
  },
  "files": [
    "bin",
    "lib",
    "run.sh",
    "scripts",
    "examples",
    "docs",
    "README.md"
  ],
  "scripts": {
    "run": "bash ./run.sh",
    "expand-env": "node ./scripts/expand-env-manifest.mjs",
    "link-global": "pnpm link --global"
  }
}
